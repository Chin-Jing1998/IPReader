# 安全说明

## 攻击面

本应用是一个**完全离线**的桌面程序：内容全部内置，运行期不发出任何外部请求，不接受任何远程输入。这消除了绝大多数常见攻击面。

具体的安全措施：

| 措施 | 实现 |
|---|---|
| 渲染进程隔离 | `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true` |
| 最小 preload 面 | 仅暴露一个布尔与一个主题 IPC，后者在主进程侧有枚举白名单校验 |
| 内容安全策略 | 由本地服务统一下发，其中 `connect-src 'self'` 把「零数据外传」变为浏览器强制 |
| 导航拦截 | 站外 http(s) 一律交系统浏览器打开，不在应用窗口内加载 |
| 路径穿越防护 | URL 先解码再规范化并校验前缀，越界返回 403 |
| DNS rebinding 防护 | 校验 Host 头，仅接受回环地址与本应用端口 |
| 只读服务 | 本地 http 服务只读文件，无任何写入路径，且仅监听 `127.0.0.1` |

上表覆盖的是应用自身的攻击面。分发环节另有一项已知限制：当前发行版未做代码签名，首次启动需用户手动放行一次，具体步骤见 README 的「安装提示」。

## 数据

批注、笔记与高亮存于浏览器 localStorage，**不离开本机**，无同步、无上传、无遥测。导出与导入均需你手动选择文件。

导入的 JSON 会经结构校验（字段类型、枚举、条目数上限）后才写入，不合规的条目跳过并如实回报数量。

## 支持的版本

仅最新发行版接受安全修复。

底层 Electron 跟随其官方支持策略（最新三个稳定大版本）。当所用版本临近终止支持时会随之升级——这可能带来最低系统要求的提升。

## 漏洞报告

发现安全问题请邮件联系 zhangjingjing962464@gmail.com（张京京）。首封邮件只需简述影响面与触发条件，收到回复后再提供完整复现步骤。
