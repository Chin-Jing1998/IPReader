# IPReader · 使用说明

## 这是什么

一座装进桌面应用的知识产权法规知识库。**76 部**规范性文件与实务文献共 **5963 个正文页**，加上 **1743 个术语词条**的关键词索引和一张全库知识图谱。

**默认完全离线**。内容全部内置，打开就能用，不需要账号。你做的批注只存在自己的电脑上。唯一会联网的是「检查更新」，它默认关着，只有你主动去点才会查一次版本号。

| 收录 | 规模 |
|---|---|
| 专利法 | 82 条 |
| 专利法实施细则 | 149 条 |
| 专利审查指南（2025） | 6 部 38 章 259 节 523 小节 |
| 侵权判定指南（2017） | 153 条 |
| 机械案件撰写规范 | 2 章 18 节 |
| 化学案件撰写规范 | 2 章 26 节 |
| 审查意见答复指引 | 7 章 26 节 |
| 司法解释与最高人民法院知识产权法庭裁判要旨 | 26 部 |
| 法律、行政法规、部门规章与规范性文件 | 43 部 |

上表前七行是主干工具书，后两行为扩展入库的 69 部，合计 76 部。完整清单见应用内首页。条文溯源覆盖 65 部法律法规的 2409 条条文正文。

76 部文献按**专利、商标、著作权、竞争法、品种布图、综合程序**六个法域归类；1743 个术语词条同样覆盖这六个法域，在关键词索引中按 41 个主题分组编排。目录树、搜索、关键词索引与知识图谱四者的书目范围一致——图谱中能看到的，正文里都读得到，反之亦然。

---

## 运行环境

- **macOS 12 (Monterey) 或更高**
- **Windows 10 或更高**

Windows 7/8.1 不再支持。这些系统所能运行的浏览器内核已停止接收安全更新两年以上，继续沿用意味着让你在一个已知有漏洞的环境里打开文档。

## 安装

本应用**没有做代码签名**。这不影响任何功能，但首次启动时系统会拦一道，需要你手动放行一次。之后每次打开都正常。

### macOS

1. 打开下载的 `IPReader-1.7.1-arm64.dmg`，把应用图标拖进「应用程序」文件夹。
2. 双击打开，系统会提示无法验证开发者——**这是预期的**，点「完成」或「取消」。
3. 打开 **系统设置 → 隐私与安全性**，向下滚动到「安全性」一栏，会看到一行提示说 IPReader 被阻止。点它旁边的 **「仍要打开」**。
4. 输入管理员密码确认。之后从启动台或程序坞正常打开即可。

> 网上流传的「右键点击 → 打开」这一招，**Apple 已在 macOS 15 (Sequoia) 中移除**，现在必须走上面的系统设置路径。

### Windows

1. 双击 `IPReader-Setup-1.7.1.exe`，按提示选择安装位置。
2. 若出现蓝色的 SmartScreen 提示（未知发布者），点 **「更多信息」**，再点 **「仍要运行」**。
3. 之后从开始菜单或桌面快捷方式打开。

---

## 界面

打开后是三栏：

- **左栏**：顶部一排三枚控件——搜索框、进入设置页的齿轮按钮、切换阅读模式的按钮；其下是目录树。
- **中栏**：正文。
- **右栏**：自上而下依次为「关系图谱」（这一页在知识图谱中的局部关联）、「目录」（本页大纲，仅在本页有小标题时出现）、「反向链接」。

正文右下角有回到顶部的按钮，外圈是本页阅读进度。

macOS 下窗口顶部另有一条标题条，**显示当前正文在目录中的位置**：自书名起，依次列出章、节直至当前小节，翻页或跳转后自动更新。窗口收窄时中间层级先收成省略号，书名与当前小节标题最后才让位。

左栏目录树的最上层只有三行：**图谱总览**、**中国**、**关键词索引**。76 部文献收在「中国」之下，先按专利、商标、著作权、竞争法、品种布图、综合程序六类权利类型分组，各类之下再按法律、行政法规、部门规章与规范性文件、司法解释与裁判规则、审查与实务指引分类排列。分组层本身只负责展开与收起，不指向具体页面；点到最末一层的文献条目才进入正文。

## 怎么用

### 搜索

点左栏搜索框或按 `Ctrl/⌘ + K`，输入即出结果，支持标题与正文全文。`↑` `↓` 在结果间移动，`Enter` 打开，右侧同步预览命中位置，`Esc` 关闭。

按 `Ctrl/⌘ + Shift + K` 切换为标签搜索。标签取自关键词索引的主题分组名，因而这条通道等于「按主题反查页面」。

### 翻页

正文底部有「上一篇 / 下一篇」，按**文档顺序**串起全部 5963 个正文页——可以像读书一样一路读下去。键盘 `←` `→` 或 `[` `]` 同样可以翻页。

### 目录树

- **条文行即正文行**。以单条条文成页的文献（如专利法各条），在目录树里不再带展开箭头，点击直接进入该条正文。
- **一键收起分两段**。第一次点击收起除当前页所在那条路径之外的全部分支，再点一次连当前路径也一并收起。
- **展开还原**。一键收起会一次抹平手工铺开的展开状态，其右侧的还原按钮可把目录树还原到收起之前的样子。
- **同级拖拽排序**。权利类型行、文件归类行与文献行都可以在同级内拖动重排，顺序记在本机。排序只影响目录树里「找书的顺序」，不影响正文「上一篇 / 下一篇」的阅读顺序。
- **恢复默认排序**。自定义过顺序后，目录树头部会出现该按钮，点击并确认即回到默认次序，展开状态不受影响。

### 阅读模式

左栏顶部第三枚按钮切换阅读模式，用于把注意力收回正文。

- 在正文页：左右两栏淡出让位，鼠标移上去仍会重新显现，随时取用。
- 在图谱总览页：左栏整条隐去，画布铺满整个窗口宽度。此时切换按钮本身随左栏一并隐去，故图谱工具条尾部会另外出现一枚**「退出阅读」**按钮作为出口。

### 术语与关键词索引

正文里带**下划线**的词是术语。点它跳到该术语的词条页，那里有释义、出处，以及与之相关的其他术语。全库共 27659 处这样的行内链接。反过来，词条页会列出这个术语在哪些条文中出现过。

左栏目录树最下方的「关键词索引」是术语的总入口：1743 个词条按 41 个主题分组，先列专利法域的 22 个主题（新颖性、创造性、权利要求、修改超范围、优先权、复审、无效宣告等），再列商标法域的 14 个主题（显著特征、近似与混淆判断、驰名商标、恶意注册与在先权利、马德里国际注册等），著作权、竞争法、品种布图、综合程序四个法域各占一组，另有一个兜底的综合组。商标段的主题粒度与专利段同构，可以按同一套习惯查检。

正文中的法条引用（如「专利法第22条」）同样自动成链，点击直达该条原文；条文页则反向列出引用过它的章节。

### 知识图谱：页内局部图

每个正文页的右栏上方是**「关系图谱」**局部图：只画与当前页直接相连的节点，点击即跳转对应页面。

在关联特别密集的页面上，局部图最多显示 120 个关联节点，并在图上给出**「已显示 N/M 个关联」**徽标（例如「已显示 120/154 个关联」）；点徽标即跳到图谱总览查看全貌。节点可以直接拖动，单击选中不会扰动已经稳定的布局。

### 知识图谱总览

左栏目录树最上方的「图谱总览」是**全库图**：76 部文献连同术语层一并成图，点任一节点即可在右侧读该知识点的正文，双击可直接跳到它的文档页，点击画布空白处清除选中。滚轮缩放、拖拽平移，节点也可以单独拖动，力导布局会实时回弹。

全景坐标在构建时已经算好，因此图谱总览打开即见成图，术语层的开关也是即时生效。

**改变窗口大小后，图谱自动重新适配到画面中**，不必再手动缩放找回全景；但若你已亲手缩放或平移过视角，则以你当前的视角为准，窗口尺寸变化不会把它拉回全景。

**工具条第一行**自左至右为四段：

1. **中国** 徽标——法域范围的第一级，当前仅收录中国。
2. **六法域标签**——「全部」加上专利、商标、著作权、竞争法、品种布图、综合程序。标签**可以多选**：先点一枚即只看该法域，再点第二枚则两片法域叠加显示，再点已亮的那枚即取消它；取消到一枚不剩、或六枚全部选满时，都会自动回到「全部」态。标签只做法域级筛选，术语层不受它影响。
3. **搜索定位**——输入节点名称，回车或点「定位」，目标节点描边高亮并平移居中。
4. **重置视图**——回到全景并清空右侧面板。阅读模式下，其右侧另有「退出阅读」。

**工具条第二行**是图例与术语层开关：

- 图例共 **30 组**，按「法域段 → 文种 → 组钮」排列：先分六个法域段，段内再按法律、法规、规章、司解、指引五类文种列出该格的组钮。主干七部文献各占一组、钮上写书名；其余各组，所在格只此一组时钮上写两字文种简称，同格有多组时才写各组自己的名称并另立文种小标题。术语组列在末位。点任一组钮即隐藏或恢复该组的全部节点。
- 图例行首的**「骨架」**段控：点一次隐藏主干七书之外的全部组、只留主干七书骨架，再点一次恢复；若此前已单独点选过其中几组，段控显示为部分隐藏的中间态。
- 术语层有独立的**三态开关：隐藏 / 弱化 / 显示**，默认隐藏，以便先看文献层、再看概念网络。

**目录抽屉**在画布左上角，按钮标注为「目录」。抽屉按「法域 → 书 → 章 → 节」三层组织，点其中任一行等同于在图里选中对应节点。

- 鼠标移上去自动展开，移开自动收起；点抽屉标题栏的**「常开」**可以锁住展开状态，此后移开鼠标也不再收起，该选择会被记住。触屏设备不启用悬停开合，仍按点击展开。
- 每行左侧的复选框可以**多选**，`Cmd/Ctrl + 点击`标题同效，最多同时选中 12 个节点；被选中的节点及其一跳邻域保持常亮，其余变暗。
- 抽屉标题栏的**「清空」**按钮一键取消全部多选，点击画布空白处同效；该按钮只在有多选时出现，钮上括号内是当前已选的节点数。
- 选中两个及以上时，右侧面板转为多选摘要，把已选节点列成一排可点的标签，逐枚可移除。

### 批注

**选中正文任意一段文字**，会浮出一个工具条：

| 按钮 | 作用 |
|---|---|
| 复制 | 复制所选文字 |
| 划线 | 加下划线 |
| 笔记 | 写一段笔记（`⌘/Ctrl + Enter` 保存，`Esc` 取消）|
| 四个色块 | 黄 / 绿 / 蓝 / 粉 高亮 |

批注**按页保存**，关掉应用再打开仍在。鼠标悬停在笔记上会显示笔记内容。

进入设置页的「批注」分类，点「批注管理」，可以看到当前页的全部批注，也可以在这里删除，或者：

- **导出全部** —— 把所有页面的批注存成一个 JSON 文件，用于备份或换电脑。
- **导入** —— 读回之前导出的文件。同一条批注取较新的那份，不会重复。

此外，在设置页的「批注」分类中可以指定一个保存目录。指定之后，桌面端会把批注自动落盘为 Markdown 文件：按标记所在章节命名，高亮与划线保留格式，笔记以脚注形式附在整段下方。这样即使不做 JSON 导出，批注也始终有一份可直接阅读的本机副本。

> 如果原文有变动（比如换了新版本的知识库），个别批注可能定位不到原位置。它们**不会被删除**，会在批注列表里灰显并注明「原文已变动」，由你决定是否删除。

### 设置与界面主题

点左栏搜索框右侧的**齿轮按钮**进入设置页。设置页左侧的抽屉把设置分为四类：

- **外观** —— 明暗模式与界面主题。明暗模式有「浅色 / 深色 / 跟随系统」三档；界面主题共六套古风配色：**宣纸**（远山淡影）、**水墨**（山水泼墨）、**青瓷**（缠枝莲纹）、**竹林**（竹枝疏影）、**暮山**（群山暮霭）、**玄夜**（星月夜山）。每套各含浅色与深色两态，随明暗模式切换；页面底色、各栏字体与图谱配色一并跟随。
- **批注** —— 指定批注的保存目录，指定后桌面端会把批注自动落盘为 Markdown 文件（见上一节「批注」）。
- **MCP** —— 接入 Claude Code、Codex 等 AI 工具的命令，按你这台电脑的实际路径生成，可一键复制（见下文「接入 AI 助手」）。
- **关于** —— 应用信息、检查更新、图谱总览与专利库的使用说明，以及作者联系方式。

抽屉左上角的「返回」回到进入设置前的页面。

### 检查更新

在设置页的**关于**分类里，版本号下方有「检查更新」按钮。点它会向 GitHub 查询一次最新版本：已是最新会直接告诉你，有新版则显示版本号与更新说明，并给出「前往下载」——点击后用系统浏览器打开发布页，由你自行下载安装。**应用不会自动下载，也不会自动安装。**

按钮下方的「启动时自动检查更新」默认**关闭**。勾选后，每次启动应用四秒左右会在后台悄悄查一次，只有确实有新版时才弹窗提醒；已是最新或当时没有网络都不会打扰你。不勾选，应用就一如既往地完全不联网。

这项设置存在本机，跟随应用而非账号——本应用没有账号。

### 接入 AI 助手（MCP）

应用内附一个 MCP 服务，随应用一并分发。接上之后，Claude Code、Codex 等支持 MCP 的 AI 工具就能直接在这 76 部法规与实务文献里检索、读原文、查术语、按条号溯源，不必再手动翻阅。

设置页的 **MCP** 分类里已按你这台电脑的实际路径生成好命令：

- **Claude Code** —— 点「复制命令」，粘进终端执行一次即可；
- **Codex CLI** —— 点「复制配置」，粘进 `~/.codex/config.toml`；
- 其他支持 MCP 的工具（如腾讯 WorkBuddy），在它自己的 MCP 管理界面按同样的命令与参数填写。

无须另装 Node，也无须下载任何东西——服务就在应用安装包里。

**要接入 Claude Code、Codex 之外的客户端**（Claude Desktop、opencode 系、Hermes、ZCode 等），源码仓库内附一个多端安装器与配套指南，见 `mcp/docs/MCP-安装指南.md`：安装器直接支持七个目标，另有十二家给出可照抄的配置片段。

接好后，AI 工具会多出十三个工具：

| 工具 | 中文名 | 用途 |
|---|---|---|
| `search_kb` | 全文检索 | 全文检索，返回命中页面的书目、层级路径与命中片段 |
| `read_node` | 读取原文 | 分页读取节点原文 |
| `browse_toc` | 浏览目录 | 按层级浏览目录树 |
| `lookup_term` | 查术语 | 查术语的释义、各书出处、关联法条与相关术语 |
| `find_law` | 查法条 | 按条号直达法条原文，并反查引用该条的章节 |
| `related_nodes` | 关联节点 | 查询知识图谱中的关联关系 |
| `list_books` | 内容清单 | 获取收录文献清单与规模 |
| `list_articles` | 条文目录 | 列出某部法的全部条号与条旨，用于通览条文骨架、核对条号 |
| `compare_articles` | 条文对照 | 并列取回多条法条正文，供同法连续条号或跨法同主题的对照 |
| `batch_read` | 批量读取 | 一次读取多个节点的摘要或正文节选，省去逐个往返 |
| `filter_books` | 筛选书目 | 按国家、法域、文献类型、效力状态筛选书目，返回域键清单与分布摘要 |
| `find_citing_sections` | 引用链反查 | 反查哪些章节引用了某法某条，按被引次数降序 |
| `get_brief` | 节点摘要 | 批量取节点的一句话摘要，供先判断再决定是否精读 |

它同样离线运行：AI 工具与这个服务之间走进程管道，不开端口、不发网络请求，检索全部在你的电脑上完成。

---

## 隐私

- 应用**默认不发出任何网络请求**。内容、图片、图标全部内置。
- 唯一的例外是设置页「关于」里的**检查更新**：默认关闭，只有你点了「检查更新」按钮、或勾选了「启动时自动检查更新」，它才会向 GitHub 查一次最新版本号。只查版本号，不下载任何文件、不自动安装、也不会把你的任何信息发出去。不想要就别勾，应用便一如既往地完全离线。
- 没有遥测、没有崩溃上报、没有使用统计、没有账号。
- 批注、笔记、高亮默认存在本机浏览器存储中；若在设置中指定了保存目录，还会落盘为该目录下的 Markdown 文件。两种存储都在你自己的电脑上，**不会离开你的电脑**。导出与导入也都需要你手动选择文件。

---

## 常见问题

**打不开，提示「已损坏」或「无法验证开发者」**
这是未签名应用的正常拦截，不是文件损坏。按上面「安装」一节的步骤放行一次即可。

**提示端口被占用**
应用固定使用本机 47821 端口。这是为了保住你的批注——浏览器按「地址 + 端口」隔离本地数据，换个端口等于换了一个空数据库，批注会看不见。请关闭占用该端口的程序后重试。

确需改端口时可设环境变量 `PATENT_KB_PORT`，但**改之前请先在原端口下导出批注**，再到新端口导入。

**批注不见了**
先确认是不是换了端口启动（见上一条）。若是在原端口下打开，检查是否曾清理过浏览器数据。建议定期用「导出全部」做备份。

**图谱总览节点太多，看不清**
全库图共 7706 个节点：文献与章节节点 5963 个，术语节点 1743 个。术语层默认隐藏，先看文献层即可。若仍嫌拥挤，有三种收窄办法：点法域标签只看一到几个法域；点图例行首的「骨架」只留主干七书；或用图例逐组隐藏不关心的部分。想看术语但不想被它淹没时，把术语层切到「弱化」而非「显示」。

**内容与官方文本不一致**
内容依据各文献的公开文本整理重构并经逐节复核，但**如与官方正式文本有出入，以官方文本为准**。本知识库不构成法律意见，不能替代对官方原文的查阅。法规会修订，使用前请确认版本是否仍现行有效。

---

## 版本

v1.7.1。许可与内容来源见随附的 `LICENSE`、`CONTENT_LICENSE.md` 与 `THIRD_PARTY_NOTICES.md`。
